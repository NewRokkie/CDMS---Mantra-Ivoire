import React from 'react';

// This file is kept for backward compatibility
// The modal functionality has been integrated into the main GateIn component

export const GateInModal: React.FC = () => {
  return null;
};

export default GateInModal;