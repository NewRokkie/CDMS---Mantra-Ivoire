# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
LOG_LEVEL=DEBUG

# File Transfer Configuration (supports both FTP and SFTP)
# The system will auto-detect protocol based on port or use explicit setting

# === DEVELOPMENT (FTP Server) ===
TRANSFER_HOST=10.80.22.118
TRANSFER_PORT=21
TRANSFER_USER=habib.sayegh
TRANSFER_PASSWORD=root
TRANSFER_REMOTE_DIR=/ftp_output
TRANSFER_PROTOCOL=auto  # Options: 'ftp', 'sftp', 'auto'

# === PRODUCTION (SFTP Server) ===
# TRANSFER_HOST=production-sftp-server.com
# TRANSFER_PORT=22
# TRANSFER_USER=edi_user
# TRANSFER_PASSWORD=secure_password
# TRANSFER_REMOTE_DIR=/incoming/edi
# TRANSFER_PROTOCOL=sftp

# === ALTERNATIVE SFTP CONFIGURATION ===
# TRANSFER_HOST=sftp.example.com
# TRANSFER_PORT=2222
# TRANSFER_USER=edi_service
# TRANSFER_PASSWORD=complex_password
# TRANSFER_REMOTE_DIR=/uploads/codeco
# TRANSFER_PROTOCOL=auto  # Will detect SFTP based on port 2222

# Output Directory
OUTPUT_DIR=./output

# Transfer Retry Configuration
TRANSFER_MAX_RETRIES=3
TRANSFER_RETRY_DELAY=1

# Legacy SFTP Configuration (for backward compatibility)
# These will be used if TRANSFER_* variables are not set
SFTP_HOST=10.80.22.118
SFTP_PORT=21
SFTP_USER=habib.sayegh
SFTP_PASSWORD=root
SFTP_REMOTE_DIR=/C/Users/habib.sayegh/Downloads/Dev Project/CDMS/dist/ftp_output
SFTP_MAX_RETRIES=3
SFTP_RETRY_DELAY=1